# 通义千问 API Key (必填)
DASHSCOPE_API_KEY=your-api-key-here

# 笔记目录路径 (可配置多个，用逗号分隔)
NOTES_DIRECTORIES=/path/to/your/notes

# 向量数据库存储路径
CHROMA_DB_PATH=../data/chroma_db

# LLM 配置
LLM_MODEL=qwen-plus
EMBEDDING_MODEL=text-embedding-v2

# 服务配置
HOST=0.0.0.0
PORT=8000
DEBUG=true
